ARG BUILD_FROM
FROM $BUILD_FROM

#ENV TS_UPDATE=1
#ENV TIME_ZONE=$TZ
ENV TS3SERVER_DB_PLUGIN=ts3db_mariadb
ENV TS3SERVER_DB_SQLCREATEPATH=create_mariadb
#ENV TS3SERVER_DB_HOST=mariadb
#ENV TS3SERVER_DB_NAME=${DB_DATABASE}
#ENV TS3SERVER_DB_USER=${DB_USER}
#ENV TS3SERVER_DB_PASSWORD=${DB_PASSWORD}
ENV TS3SERVER_DB_WAITUNTILREADY=30
ENV TS3SERVER_LICENSE=accept
# Copy data for add-on
COPY run.sh /
RUN chmod a+x /run.sh

WORKDIR /teamspeak

CMD [ "/run.sh" ]